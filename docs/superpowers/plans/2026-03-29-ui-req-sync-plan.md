# 文档同步计划：UI_Requirement ← REQ_03

## 任务目标

将 REQ_03 第4节中 UI_Requirement 缺失的内容同步到 UI_Requirement 5.8 节。

## 需要同步的内容

### 1. 新增章节（在 5.8.11 之后插入）

| 新章节 | 来源 | 内容 |
|--------|------|------|
| 5.8.12 功能需求与API | REQ_03 4.12 | 后端 API 端点定义 |
| 5.8.13 任务条显示字段 | REQ_03 4.13 | 任务条可配置显示字段 |
| 5.8.14 业务规则 | REQ_03 4.14 | TL-RULE-01~04 |

### 2. 章节编号调整

| 原编号 | 新编号 | 章节名称 |
|--------|--------|----------|
| 5.8.12 | 5.8.15 | 数据类型定义 |
| 5.8.13 | 5.8.16 | 时间轴操作API |
| 5.8.14 | 5.8.17 | 响应式适配 |
| 5.8.15 | 5.8.18 | 周末/节假日显示规范 |
| 5.8.16 | 5.8.19 | 看板统计时间线规范 |

## 同步内容详情

### 5.8.12 功能需求与API

```markdown
### 5.8.12 功能需求与API

| 功能 | 描述 | 优先级 | API端点 |
|------|------|--------|---------|
| 创建时间轴 | 为项目创建多条独立时间轴 | P0 | POST /api/projects/:id/timelines |
| 删除时间轴 | 删除指定时间轴及其任务 | P0 | DELETE /api/timelines/:id |
| 创建任务节点 | 在时间轴上创建任务 | P0 | POST /api/timelines/:id/tasks |
| 编辑任务节点 | 修改任务信息 | P0 | PUT /api/timeline-tasks/:id |
| 删除任务节点 | 删除任务节点 | P0 | DELETE /api/timeline-tasks/:id |
| 拖拽调整时间 | 通过拖拽调整任务时间 | P0 | PUT /api/timeline-tasks/:id |
| 自动排列 | 智能调整任务位置避免重叠 | P1 | - |
```

### 5.8.13 任务条显示字段（可配置）

```markdown
### 5.8.13 任务条显示字段（可配置）

| 字段 | 默认显示 | 说明 |
|------|----------|------|
| 任务名称 | ✅ | 必须显示 |
| 进度百分比 | 可选 | 显示进度条 |
| 负责人头像 | 可选 | 显示头像图标 |
| 状态颜色 | ✅ | 根据状态自动着色 |
```

### 5.8.14 业务规则

```markdown
### 5.8.14 业务规则

| 规则ID | 规则名称 | 描述 |
|--------|---------|------|
| TL-RULE-01 | 时间轴归属 | 时间轴属于特定项目 |
| TL-RULE-02 | 任务归属 | 任务节点属于特定时间轴 |
| TL-RULE-03 | 日期约束 | 任务开始日期不能晚于结束日期 |
| TL-RULE-04 | 范围约束 | 任务日期应在时间轴日期范围内 |
```

## 执行步骤

1. 在 5.8.11 删除确认对话框后插入三个新章节（5.8.12、5.8.13、5.8.14）
2. 将原 5.8.12 数据类型定义改为 5.8.15
3. 将原 5.8.13 时间轴操作API改为 5.8.16
4. 将原 5.8.14 响应式适配改为 5.8.17
5. 将原 5.8.15 周末/节假日显示规范改为 5.8.18
6. 将原 5.8.16 看板统计时间线规范改为 5.8.19

## 影响范围

- 文件: `docs/requirements/UI_Requirement_0323-1921.md`
- 修改位置: 第 4188-4400 行左右
- 新增内容: 约 30 行
