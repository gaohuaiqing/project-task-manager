#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import re

# 读取文件
with open('docs/requirements/UI_Requirement_0323-1921.md', 'r', encoding='utf-8') as f:
    content = f.read()

# 查找并替换时间线模块开头
old_text = '''## 5.8 时间线管理模块 ✅ 用户确认 (2026-03-28 更新)

> **入口**: 项目详情页 → 时间线Tab
> **需求来源**: FINAL_REQUIREMENTS 模块#4 时间线管理

### 5.8.1 整体布局结构'''

new_text = '''## 5.8 时间线管理模块 ✅ 用户确认 (2026-03-28 二次确认)

> **入口**: 项目详情页 → 时间线Tab
> **需求来源**: FINAL_REQUIREMENTS 模块#4 时间线管理

### 2026-03-28 时间线界面二次确认

| 设计项 | 用户选择 |
|--------|----------|
| 左侧区域 | 时间轴名称列表（固定宽度 180px） |
| 时间轴排列 | 上下堆叠，每条时间轴占一行 |
| 任务条排列 | 重叠在同一行，自动错开避免遮挡 |
| 任务条内容 | 可配置，用户选择显示字段 |
| 任务交互 | 点击任务条跳转到任务详情页 |
| 日期刻度 | 根据缩放自动调整（月→周→日） |
| 默认范围 | 自动适应项目日期范围 |
| 里程碑位置 | 时间线上方独立一行 |
| 里程碑图标 | 旗帜（flag）图标 |
| 里程碑文字 | **名称在横线上方，日期在横线下方** |
| 节假日标记 | 斜线/网格纹理 |
| 底部工具栏 | 缩放控制 + 今天按钮 |

### 里程碑展示示意

```
┌──────────┬──────────────────────────────────────────────────────────────────────┐
│          │        🚩需求评审     🚩设计完成          🚩上线    ← 名称在横线上方   │
│ 🚩 里程碑 │  ─────────────────────────────────────────────────────────────────────── │
│          │          3/15            4/1               5/20      ← 日期在横线下方   │
└──────────┴──────────────────────────────────────────────────────────────────────┘
```

### 5.8.1 整体布局结构'''

if old_text in content:
    content = content.replace(old_text, new_text)
    with open('docs/requirements/UI_Requirement_0323-1921.md', 'w', encoding='utf-8') as f:
        f.write(content)
    print('UI需求文档更新成功')
else:
    print('未找到目标文本，尝试查找...')
    # 尝试找到实际的文本
    lines = content.split('\n')
    for i, range(len(lines)):
        if '5.8' in lines[i] and '时间线' in lines[i]:
            print(f"第{i}行: {lines[i][:80]}")
            if i < len(lines) - 3:
                print(f"第{i+1}行: {lines[i+1][:80]}")
                print(f"第{i+2}行: {lines[i+2][:80]}")
